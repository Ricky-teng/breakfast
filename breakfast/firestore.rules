rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Orders collection: allow create by anyone (no read of others' orders list in client by default)
    match /orders/{orderId} {
      allow create: if true;
      allow read, update, delete: if request.auth != null; // admin app should use auth
    }
  }
}
